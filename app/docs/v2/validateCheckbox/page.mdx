---
title: "validateCheckbox"
description: "Validates a checkbox input field and returns an ErrorMessageInterface."
---

# validateCheckbox

The `validateCheckbox` function validates a specified checkbox input field. It checks if the checkbox is checked and returns an [ErrorMessageInterface](/docs/v2/errorMessageInterface).

**Works in:** HTML5, React JS

| Parameter | Type | Default | Description |
| --- | --- | --- | --- |
| `checkboxInputField` | `HTMLInputElement` | `undefined` | The checkbox input field to validate. |
| `options` | `object` | `{}` | Optional settings for validation such as custom error messages. |

## Usage

### Example: Validate a Checkbox Input

```js
// Using with a checkbox input element
const checkboxElement = document.querySelector('input[type="checkbox"]');
const result = nfsfu234FormValidation.validateCheckbox(checkboxElement, {
  message: 'You must agree to the terms and conditions.'
});
console.log(result);
```

#### See also

- [customErrorMessageInterface](/docs/v2/customErrorMessageInterface)
- [ErrorMessageInterface](/docs/v2/errorMessageInterface)
- [displayError](/docs/v2/displayError)
- [validateAllCheckbox](/docs/v2/validateAllCheckbox)
